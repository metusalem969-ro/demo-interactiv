image: alpine:3.20

stages:
  - deploy

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html manifest.json sw.js icon.svg .nojekyll public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
